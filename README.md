# Network interface traffic logger

```plain
Network interface traffic logger

Usage:
  gomon [command]

Available Commands:
  completion  Generate the autocompletion script for the specified shell
  help        Help about any command
  log         Log network interface traffic to a file

Flags:
  -h, --help     help for gomon
  -t, --toggle   Help message for toggle

Use "gomon [command] --help" for more information about a command.
```
